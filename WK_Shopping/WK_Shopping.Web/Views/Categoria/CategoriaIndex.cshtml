@model IEnumerable<WK_Shopping.Web.Models.CategoriaModel>

@{
    ViewData["Title"] = "Categorias";
}

<br/>
<br/>
<div class="container p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">Lista de Categorias</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-action="CategoriaCreate" class="btn btn-outline-primary">
                <i class="fas fa-plus"></i>Nova Categoria
            </a>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    Código
                </th>
                <th>
                    Nome
                </th>
                <th>
                    Ação
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Id
                    </td>
                    <td>
                        @item.Nome
                    </td>
                    <td>
                       <a asp-action="CategoriaUpdate" class="btn btn-success" asp-route-id="@item.Id">
                           <i class="fas fa-edit"></i>
                       </a>
                       <a asp-action="CategoriaDelete" class="btn btn-danger" asp-route-id="@item.Id">
                           <i class="fas fa-trash"></i>
                       </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>